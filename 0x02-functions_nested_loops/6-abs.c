#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 *
 * @n: number to be computed for absolute value
 *
 * Return: 0 Success
 */
int _abs(int n)
{
	return(((n >= 0) ? n: n* -1));
}
